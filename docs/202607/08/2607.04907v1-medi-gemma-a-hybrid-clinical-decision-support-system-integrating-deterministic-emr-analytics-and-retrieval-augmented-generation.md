---
title: "Medi-Gemma: A Hybrid Clinical Decision Support System Integrating Deterministic EMR Analytics and Retrieval-Augmented Generation"
title_zh: Medi-Gemma：一种整合确定性EMR分析与检索增强生成的混合临床决策支持系统
authors: "Mohammed Saim Ahmed Quadri, Yunzhe Xue, Justin W. Ady, Usman Roshan"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04907v1"
tags: ["query:agent"]
score: 8.0
evidence: 针对伤口病理的临床决策支持系统，采用智能体架构
tldr: 大语言模型在临床应用中面临结构幻觉和对表格数据推理弱的挑战。本文提出Medi-Gemma系统，采用解耦框架分离临床感知与数据编排，并通过地面真值注入模块强制LLM基于最新结构化电子病历状态进行推理。验证表明该方法消除了语义上下文漂移，提高了对临床存储库的事实遵循。该系统为高保真度和确定性安全的临床决策支持提供了可行模式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04907v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1158, \"height\": 1788, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04907v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1890, \"height\": 993, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04907v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 937, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04907v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 705, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04907v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 360, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04907v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 927, \"height\": 452, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04907v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 852, \"height\": 354, \"label\": \"Table\"}]"
motivation: 现有LLM在临床场景因结构幻觉和弱确定性推理而受限，需一种能融合结构化EMR分析与检索增强生成的混合系统。
method: 构建多阶段管线：DataManager清洗EMR，IntentRouter路由请求，Ground Truth Injection模块提取患者标识并注入最新临床状态作为LLM提示覆盖上下文，配合ProtocolManager确保安全。
result: 验证显示系统消除语义漂移，避免数据库崩溃，并显著提升临床事实遵循度。
conclusion: Medi-Gemma为基于LLM的临床决策支持提供了更安全的设计，确保数据保真度和确定性安全。
---

## 摘要
在高风险的临床环境中部署大型语言模型（LLMs）仍受到结构性幻觉、对表格患者数据的弱确定性推理以及向量检索遗漏的限制。本文介绍了Medi-Gemma的架构与验证，这是一种用于伤口病理分诊和工作流程自动化的临床决策支持系统（CDSS）。该平台引入了解耦框架，将临床感知与数据编排分离，同时保留可追溯的推理。Medi-Gemma使用由集中式ClinicalOrchestrator协调的多阶段流水线。DataManager通过类型强制清理非结构化电子病历（EMR）文件，处理数据请求而无需生成式推理。自然语言查询由分层IntentRouter处理，该路由器将请求路由到由PandasQueryEngine执行的确定性分析路径，或路由到由ClinicalRAGEngine管理的患者特定推理，后者使用CPU优化的向量存储。一个关键贡献是Ground Truth Injection模块，该模块拦截患者特定查询，提取数字识别令牌，通过Pandas查询结构化数据框，检索最新的已验证临床状态，并在生成前将此快照作为覆盖上下文块嵌入LLM提示中。安全性合规由确定性ProtocolManager强制执行，该管理器将临床术语映射到固定的基于证据的风险路径，而SafetyVerifier短语过滤器防止输出规则违规。验证表明，该架构消除了语义上下文漂移，防止了数据库编译崩溃，并提高了对后端临床存储库的事实依从性。这些结果支持Medi-Gemma作为基于LLM的临床决策支持的一种更安全的模式，其中结构化数据保真度、检索基础和确定性保障至关重要。

## Abstract
Deploying Large Language Models (LLMs) in high-stakes clinical settings remains limited by structural hallucinations, weak deterministic reasoning over tabular patient data, and omissions in vector retrieval. This paper presents the architecture and validation of Medi-Gemma, a Clinical Decision Support System (CDSS) for wound pathology triage and workflow automation. The platform introduces a decoupled framework that separates clinical perception from data orchestration while preserving traceable reasoning. Medi-Gemma uses a multi-stage pipeline coordinated by a centralized ClinicalOrchestrator. Data requests are handled without generative inference by a DataManager that cleans unstructured Electronic Medical Record (EMR) files through type coercion. Natural language queries are processed by a hierarchical IntentRouter, which routes requests to deterministic analytics paths executed by a PandasQueryEngine or to patient-specific reasoning managed by a ClinicalRAGEngine using a CPU-optimized vector store. A key contribution is the Ground Truth Injection Module, which intercepts patient-specific queries, extracts numeric identification tokens, queries the structured dataframe via Pandas, retrieves the latest validated clinical state, and embeds this snapshot as an overriding context block in the LLM prompt before generation. Safety compliance is enforced by a deterministic ProtocolManager that maps clinical terminology to fixed evidence-based risk pathways, while a SafetyVerifier phrase filter prevents output rule violations. Validation shows that this architecture eliminates semantic context drift, prevents database compilation crashes, and improves factual adherence to backend clinical repositories. These results support Medi-Gemma as a safer pattern for LLM-based clinical decision support where structured data fidelity, retrieval grounding, and deterministic safeguards are essential.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLMs）在临床决策支持系统中存在两大核心缺陷：一是**结构幻觉**（对表格数据的定量推理产生虚构结果），二是**向量检索遗漏**（传统RAG在纵向患者跟踪中可能忽略最新或关键的历史上下文）。
- **研究动机**：如何在保证**结构化电子病历（EMR）数据保真度**的同时，借助LLM的语义理解能力，构建安全、可追溯的临床决策支持系统。
- **整体含义**：提出一种混合架构，将确定性计算（如Pandas数据分析）与检索增强生成（RAG）分离，通过**地面真值状态注入**确保LLM始终基于最新验证的患者状态进行推理，降低幻觉风险。

## 2. 方法论：核心思想、关键技术细节与流程

### 2.1 核心思想
- **解耦框架**：将临床感知（语义理解）与数据编排（确定性分析）分离，保留可追溯推理。
- **地面真值注入（Ground Truth Injection）**：在LLM推理前，强制注入最新结构化患者状态，作为覆盖性上下文。

### 2.2 系统架构与关键组件
- **ClinicalOrchestrator**：集中式协调器，管理整个流水线。
- **DataManager**：负责EMR数据预处理，通过类型强制（如将数字字段中的“N/A”转换为null）统一格式，防止数据库编译崩溃。
- **IntentRouter**：分层意图路由器，基于规则优先级分类查询：
  1. 若包含患者标识 → 路由到**临床路径**（Clinical Route）。
  2. 若含聚合关键词（如“average”、“count”）→ 路由到**数据路径**（Data Route）。
  3. 若包含上下文引用（如“this wound”）→ 路由到临床路径。
- **PandasQueryEngine**：处理数据路径查询，生成Python代码执行精确计算（如`.mean()`、`.count()`），避免算术幻觉。
- **ClinicalRAGEngine**：处理临床路径查询，基于CPU优化向量存储（BAAI/bge-small-en-v1.5模型）进行语义检索，并通过MetadataFilters限制仅检索目标患者文档。
- **GroundTruth Injection模块**：
  - 提取患者ID → Pandas精确查找最新记录 → 提取关键参数（日期、伤口尺寸、组织成分等）→ 嵌入LLM提示作为覆盖上下文块，附带强制指令（`[SYSTEM INJECTED GROUND TRUTH - PRIORITY OVER RETRIEVAL]`）。
- **ProtocolManager**：基于预定义配置文件（如`protocols.yaml`）的规则映射，将症状术语映射到固定风险路径（如“necrotizing fasciitis”映射为Critical Risk）。
- **SafetyVerifier**：关键词过滤器，检查并屏蔽违规输出。

### 2.3 流程概述
1. 数据上传与清洗（DataManager）。
2. 用户输入查询 → IntentRouter分类。
3. 数据查询 → PandasQueryEngine直接执行；临床查询 → ClinicalOrchestrator触发地面真值注入 → ClinicalRAGEngine补充语义检索。
4. ProtocolManager应用协议映射 → SafetyVerifier验证输出 → 返回结果。

## 3. 实验设计：数据集、场景与对比方法

- **数据集**：使用**代表性去标识化EMR数据集**，包含结构化数值（伤口尺寸、组织成分、疼痛等级等）和半结构化临床记录。未提及具体公开数据集名称或规模。
- **场景**：
  - EMR数据预处理验证（标准化、类型强制）。
  - 动态意图路由验证（统计查询 vs. 患者查询）。
  - 地面真值注入验证（对比传统RAG仅依赖语义检索的缺陷）。
  - 混合临床决策流水线验证（端到端工作流）。
- **Benchmark**：未使用标准医学问答基准（如MedQA）。主要对比对象是**传统RAG系统**（表I列出了能力对比：语义检索、结构化分析、意图路由、地面真值注入、协议推理、安全验证、仪表盘分析、多模态分析等）。
- **对比方法**：未明确列出具体基线模型，仅定性比较“Conventional RAG”与Medi-Gemma的能力差异。

## 4. 资源与算力

- **未明确说明具体GPU型号、数量或训练时长**。
- 提到**ClinicalRAGEngine使用CPU优化的向量存储**（BAAI/bge-small-en-v1.5模型运行于CPU），且LLM推理通过Ollama本地部署（可能使用Gemma系列模型）。未提供训练/推理硬件细节。
- 实验性质为**系统验证**而非大规模模型训练，因此算力需求相对较低。

## 5. 实验数量与充分性

- **实验数量**：定性描述了四个方面的验证（数据预处理、意图路由、地面真值注入、混合流水线），但**未提供定量实验指标**（如准确率、召回率、F1分数等）。
- **充分性**：实验覆盖了架构各组件功能验证，但**缺乏消融实验**（如移除地面真值注入或意图路由后的性能对比）、**缺乏与传统RAG或纯LLM方法的量化比较**（如幻觉率、事实一致性分数）。
- **客观性**：使用“代表性临床工作流”进行验证，但未公开测试查询集或样本量，存在**选择偏差风险**。结论主要基于定性观察（如“eliminates semantic context drift”），缺乏统计显著性检验。

## 6. 主要结论与发现

- Medi-Gemma架构成功**消除了语义上下文漂移**（通过地面真值注入强制LLM关注最新状态）。
- **防止数据库编译崩溃**（DataManager的类型强制转换）。
- **提高了对后端临床存储库的事实遵循度**（确定性计算与注入机制）。
- 分离确定性计算与生成式推理是**更安全的临床AI模式**，尤其在数据保真度、检索基础和安全保障至关重要的场景。

## 7. 优点：方法或实验设计亮点

- **创新架构**：将意图路由器、地面真值注入、协议安全层有机结合，形成透明、可追溯的流水线。
- **确定性优先**：所有数值计算绕过LLM直接由代码执行，从根源消除算术幻觉。
- **模块化设计**：各组件可独立替换或升级（如更换LLM、向量模型或协议库）。
- **数据隐私友好**：向量模型和LLM均支持本地CPU部署，无需上传数据至云端。
- **安全层完备**：ProtocolManager与SafetyVerifier实现双层防护，可防止临床风险误判和输出违反规则。

## 8. 不足与局限

- **缺乏定量实验**：未报告任何客观指标（如Hallucination Rate、Factuality Score、Latency等），仅定性描述。
- **未进行临床前瞻性评估**：仅在模拟数据集上验证，未在真实医院环境、真实临床医生交互中测试。
- **通用性受限**：当前聚焦伤口护理领域；虽声称可泛化，但需扩展协议库和本体论才适用于其他专科。
- **依赖EMR质量**：地面真值注入的数据准确性完全依赖于底层EMR的完整性与及时性；若EMR本身缺失或过时，推理仍会受限。
- **缺乏消融研究**：未量化每个组件（如IntentRouter、GroundTruth Injection）的独立贡献。
- **未与当前主流医学LLM基准对比**：如未比较Med-PaLM、LLaVA-Med等模型，无法说明该系统相比纯LLM方法的绝对优势。
- **计算资源未公开**：无法评估系统的可复现性或资源开销。

（完）
